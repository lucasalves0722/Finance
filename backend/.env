DB_HOST=localhost       # Se estiver rodando o DB diretamente na sua máquina
# OU
# DB_HOST=postgres      # Se estiver rodando o DB via Docker (docker-compose)
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=seu_banco
DB_PORT=5432